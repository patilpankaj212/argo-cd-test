apiVersion: v1
kind: Service
metadata:
  name: argo-test-service
  labels:
    app: nginx
  namespace: default
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: nginx